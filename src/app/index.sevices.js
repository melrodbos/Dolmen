// ;(function(){
//   'use strict';
//
//   var services = angular.module( 'dolmen.services', ['firebase']);

  // services.factory( 'authServices', function( $state, $firebaseAuth ){
    // FIXME: Move all of this code into `pologin.controller.js:PoLoginController`
      // authServices = {};
      //
      // var ref = new Firebase( 'https://dolmen.firebaseio.com' );
      // authServices.authObj = $firebaseAuth( ref );
      //
      // authServices.login = function()
      // {
      //   authServices.authObj.$authWithOAuthPopup( 'google')
      //     .then(function(ref){
      //       $state.go('po_dash');
      //     })
      //     .catch( function( err ){
      //       // console.log( err );
      //     });
      // };
  //   }
  // );

// })();
